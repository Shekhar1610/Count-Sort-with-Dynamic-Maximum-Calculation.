# Count-Sort-with-Dynamic-Maximum-Calculation.
This implementation of Count Sort addresses a special condition where the maximum value in the input data is not given explicitly. Instead, the algorithm dynamically calculates the maximum value from the input data during its execution.
